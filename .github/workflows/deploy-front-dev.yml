name: Deploy dev (ElectroGases Front)

on:
  push:
    branches: [development]

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    outputs:
      owner_lc: ${{ steps.lowercase.outputs.OWNER_LC }}
    steps:
      - uses: actions/checkout@v4

      - name: Lowercase owner
        id: lowercase
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push front image (dev)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ steps.lowercase.outputs.OWNER_LC }}/electrogases-web:dev-latest
            ghcr.io/${{ steps.lowercase.outputs.OWNER_LC }}/electrogases-web:dev-${{ github.sha }}
          build-args: |
            VITE_URL_API_DEV=${{ secrets.VITE_URL_API_DEV }}
            VITE_INSPECTOR=${{ secrets.VITE_INSPECTOR }}

  deploy:
    name: Remote Deploy via SSH
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Remote deploy (SSH)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd /var/www/proyectos/jecgas-landing-dev
            docker compose pull web
            docker compose up -d --force-recreate web
            docker image prune -a -f